A la compilation, on ne peut pas savoir quel est le type réel d'un pbjet (on a que son type déclaré => générer du code pour l'avoir à exécution).

toutes les classes sont dans le même fichier.
toutes les classes sont définies avant d'être utilisées (idem pour les attributs et méthodes).

Pas besoin de tests avec des erreurs qui violent les contraintes imposées (les N.B).

Pas besoin d'implanter la surcharge.
Eclipse : plugin EGG
	=> install new software
	=> EGG
	   http://gandriau.perso.enseeiht.fr/egg-update-site
	=> [] EGG DEV...
	   [] decocher group item by category
